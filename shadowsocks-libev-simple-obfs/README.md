#### Build

```shell
docker build --rm --tag ovear/shadowsocks-libev https://github.com/Ovear/Note.git#:shadowsocks-libev-simple-obfs
```

#### Note

Add simple-obfs(discontinued) to shadowsocks-libev official docker image.